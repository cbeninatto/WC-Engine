anthropic>=0.40
openpyxl>=3.1
httpx>=0.27
python-dotenv>=1.0
fastapi>=0.115
uvicorn>=0.30
psycopg[binary]>=3.1   # Postgres/Supabase backend (used only when DATABASE_URL is set)
scipy>=1.11            # tuner only (agents/tuner.py); not used by the web app
